trigger newMembershipStaging on Fon_Membership_Staging__c (before insert) {
   // if(trigger.isInsert && trigger.isBefore){
        
     //  newMembershipStagingClass.createMembershipStaging(trigger.new); 
   // }
}